import React, { ReactElement } from 'react'
import { TranslationNavigationProps } from './types'

const TranslationNavigation = (_props: TranslationNavigationProps): JSX.Element => {
  return <div>
    Translation Navigation Props
  </div>
}

export default TranslationNavigation
